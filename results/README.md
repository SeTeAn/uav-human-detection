# Preserved Experiment Results

This directory stores metrics and selected visual examples from the saved experiment.

The trained checkpoint that produced these metrics was not preserved, so the values should be treated as archived experiment results rather than fully reproducible results from this repository alone.

Annotated prediction examples are stored in `examples/`.
